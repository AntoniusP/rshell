#include "cmdComponent.h"

    bool isParsed(){
        
    }
    void parse(){
        
    }
    bool is_connector(){
        if(keyword=="||" || keyword=="&&" || keyword == ";") return true;
        else return false;
    }
    string getWord(){
        
    }